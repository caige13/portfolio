# Dev-only: works around CRA 5's "allowedHosts[0] should be a non-empty string"
# bug that triggers when "proxy" is set (webpack-dev-server can't infer a LAN URL).
# Has no effect on production builds.
DANGEROUSLY_DISABLE_HOST_CHECK=true
